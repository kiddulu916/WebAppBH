{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=\"$CLAUDE_FILE_PATH\"; if [ -n \"$file\" ] && echo \"$file\" | grep -qE '/dashboard/.*\\.(ts|tsx)$'; then cd /home/kiddulu/Projects/WebAppBH/dashboard && npx eslint --fix \"$file\" 2>/dev/null; fi",
            "timeout": 10000
          },
          {
            "type": "command",
            "command": "file=\"$CLAUDE_FILE_PATH\"; if [ -n \"$file\" ] && echo \"$file\" | grep -qE '/workers/[^/]+/(base_tool|pipeline)\\.py$|/workers/[^/]+/tools/[^/]+\\.py$'; then echo 'SECURITY-SENSITIVE FILE MODIFIED: Run the security-reviewer agent on this file before continuing.' >&2; fi",
            "timeout": 5000
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash(git commit*)",
        "hooks": [
          {
            "type": "command",
            "command": "FLAG=/tmp/.webbh-code-reviewed; if [ -f \"$FLAG\" ]; then rm -f \"$FLAG\"; exit 0; else echo 'CODE REVIEW REMINDER (one-shot ritual): Run the code-reviewer agent on staged changes, then retry the commit. Re-running git commit proceeds; this hook does not verify the agent actually ran.' >&2; touch \"$FLAG\"; exit 1; fi",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
